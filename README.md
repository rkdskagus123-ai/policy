# Public Policy Pages

This repository hosts public policy pages for apps published by `nhkang`.

## Apps

- Jungdon / 정돈: `https://rkdskagus123-ai.github.io/policy/jungdon/`

## Structure

Each app has its own folder so future apps can publish separate policy pages
without changing existing Play Console URLs.

```txt
/
  index.html
  jungdon/
    index.html
```
